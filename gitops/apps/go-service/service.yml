apiVersion: v1
kind: Service
metadata:
  name: go-service
  namespace: go-service
spec:
  type: ClusterIP
  selector:
    app: go-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
