apiVersion: v1
kind: Service
metadata:
  name: node-service
  namespace: node-service
  labels:
    app: go-service
spec:
  type: ClusterIP
  selector:
    app: node-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
